# VotingPresetPlugin-AC

AssettoServer plugin for preset/track voting. Fork maintained for Redline Racing; built to live under `serv-game/plugins/` and reference the parent AssettoServer solution.

Enable in server config with `VotingPresetPlugin` in `EnablePlugins`. Configure via `plugin_voting_preset_cfg.yml` in each preset folder and in cfg.
